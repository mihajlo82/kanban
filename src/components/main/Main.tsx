import CardSections from "../cardSection/CardSections";

const Main = () => {
  return (
    <main className="w-screen h-screen flex flex-row justify-center	">
      <CardSections />
    </main>
  );
};

export default Main;
