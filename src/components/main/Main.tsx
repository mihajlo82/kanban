import CardSections from "../cardSection/CardSections";

const Main = () => {
  return (
    <main className="w-screen bg-sky-500/100 h-screen flex flex-row justify-center	">
      <CardSections />
    </main>
  );
};

export default Main;
